# WP Failed Login Notifier

WordPress Plugin to notify on failed logins (by default) per e-mail using the [Monolog](https://github.com/Seldaek/monolog) logger.

## Available Filters

 * `fln_options`
 * `fln_log_handler`
